---
- name: Backup running config
  hosts: all
  gather_facts: false
  tasks:
    - name: Backup running config
      cisco.ios.ios_config:
        backup: true
        backup_options:
          dir_path: /home/mmrfai/netauto-smkn1/backup-otomatis/{{ inventory_hostname }}/
      tags: otomatis, manual, backup
